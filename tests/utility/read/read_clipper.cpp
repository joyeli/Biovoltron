#include <biovoltron/utility/read/read_clipper.hpp>
#include <iostream> //debug
#include <catch.hpp>

TEST_CASE("Read clipper") {

    SECTION("hard_clip_soft_clipped_bases") {
        std::cout << "TODO: utility/read/read_clipper.hpp requires test: hard_clip_soft_clipped_bases" << std::endl;
    }

    SECTION("revert_soft_clipped_bases") {
        std::cout << "TODO: utility/read/read_clipper.hpp requires test: revert_soft_clipped_bases" << std::endl;
    }
    
    SECTION("hard_clip_to_interval") {
        std::cout << "TODO: utility/read/read_clipper.hpp requires test: hard_clip_to_interval" << std::endl;
    }
}

